apiVersion: v1
kind: Service
metadata:
  name: {{ include "fluxor.fullname" . }}
  labels:
    {{- include "fluxor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fluxor.selectorLabels" . | nindent 4 }}
